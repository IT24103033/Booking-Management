# Book Manager Backend

## Setup
1. Run `npm install`
2. Copy `.env.example` to `.env`
3. Add your MongoDB Atlas connection string to `MONGO_URI`
4. Run `npm run dev`

## API Endpoints
- `GET /api/books`
- `POST /api/books`
- `PUT /api/books/:id`
- `DELETE /api/books/:id`
